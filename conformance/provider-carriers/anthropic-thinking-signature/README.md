# anthropic-thinking-signature

Anthropic provider-carrier fixture for a thinking block carrying a
provider signature.

The canonical semantic layer records the human-readable reasoning block.
The carrier layer preserves the exact Anthropic thinking block so a
later `anthropic.messages` projection can replay the provider-native
shape byte-for-byte.
