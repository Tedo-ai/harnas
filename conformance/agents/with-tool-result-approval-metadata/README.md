# with-tool-result-approval-metadata

Locks the v0.19.0 optional `tool_result.payload.approval` metadata shape.
Every allowed decision value round-trips byte-identically. The fixture appends
shape-only events directly because approval decision-making remains product
policy; Harnas only standardizes persisted metadata.
