# with-subagent-parallel

Exercises two sibling child sessions. The parent records two spawns and
receives terminal results in reverse order, so projections must be
order-independent and still report children in spawn order.
