# with-revert-chain

Exercises :compact + :revert composition. The fixture appends a compact mutation, appends a revert for it, then asserts the following provider request sees the original events again.
