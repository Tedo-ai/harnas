# with-subagent-nested

Exercises recursive delegation: parent to child, then child to
grandchild. The fixture verifies delegation-chain metadata and the
cross-session descendant timeline projection.
